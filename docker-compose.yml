version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    restart: unless-stopped
    ports:
      - "${PORT}:${PORT}"
    environment:
      APP_ENV: ${APP_ENV}
      PORT: ${PORT}
      BLUEPRINT_DB_HOST: ${BLUEPRINT_DB_HOST}       # e.g., lpr-postgres
      BLUEPRINT_DB_PORT: ${BLUEPRINT_DB_PORT}       # e.g., 5432
      BLUEPRINT_DB_DATABASE: ${BLUEPRINT_DB_DATABASE}
      BLUEPRINT_DB_USERNAME: ${BLUEPRINT_DB_USERNAME}
      BLUEPRINT_DB_PASSWORD: ${BLUEPRINT_DB_PASSWORD}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT}
      MINIO_PUBLIC_ENDPOINT: ${MINIO_PUBLIC_ENDPOINT}
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY}
      MINIO_USE_SSL: ${MINIO_USE_SSL}
      MINIO_BUCKET_IMAGE_LPR: ${MINIO_BUCKET_IMAGE_LPR}
      LPR_API_KEY: ${LPR_API_KEY}
    networks:
      - lpr-network   # ✅ just reference the network name
      - post_transaction-network
      - minio_default

volumes:
  postgres_data_bp:

networks:
  lpr-network:
    external: true   # ✅ define external network only at top level
    
  post_transaction-network:
    external: true
  
  minio_default:
    external: true